import { styled } from "styled-components";

export const DivLauncher = styled.div`
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 50vw;
  /* max-width: 35%;
  height: 100%;
  margin-top: 5%;
  margin-left: 5%;
  align-items: center;
  color: white;
  background-color: #83898f; */
  /* &:hover {
    cursor: pointer;
    border: 1px solid black;
    background: rgba(0, 0, 0, 0.1);
  } */
`;
